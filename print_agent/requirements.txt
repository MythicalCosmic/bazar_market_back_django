websockets>=15.0
python-escpos>=3.1
pyusb>=1.3.1
pillow>=12.0
pyinstaller>=6.0
customtkinter>=5.2.2
pywin32>=306; sys_platform == "win32"
# Tkinter (the GUI base) ships with the standard CPython installer — no pip needed.
